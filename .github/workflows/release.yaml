name: Release

on:
  release:
    types: [published]

jobs:
  checks:
    name: Version check
    runs-on: ubuntu-latest
    steps:
      - name: Check version
        run: '[ "${{ release.tag_name }}" == `poetry version` ]'

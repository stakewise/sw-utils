name: Release

on:
  release:
    types: [published]

jobs:
  checks:
    name: Version check
    runs-on: ubuntu-latest
    steps:
      - name: Check version
        run: '[ "${{ github.ref_name }}" == `poetry version` ]'
